

spring.application.name=fieldforcepro-backend


# ==== DataSource (MySQL) ====
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:mysql://localhost:3306/fieldforce}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:root}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:root}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ==== JPA / Hibernate ====
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=true 

# Email Configuration
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=worldtriplink30@gmail.com
spring.mail.password=gulk awik boqf mhrr
spring.mail.protocol=smtp

spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.ssl.enable=false

# ==== Flyway ====
spring.flyway.enabled=false
spring.flyway.locations=classpath:db/migration

# ==== Redis / Session ====
spring.redis.host=${SPRING_REDIS_HOST:localhost}
spring.redis.port=${SPRING_REDIS_PORT:6379}
spring.session.store-type=redis

# ==== Server ====
server.port=8080
server.servlet.context-path=/api/v1

# ==== File Upload (for attendance & product images) ====
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# ==== Logging ====
logging.level.root=INFO

# ==== Swagger / OpenAPI ====
springdoc.api-docs.path=/v3/api-docs
springdoc.swagger-ui.path=/swagger-ui/index.html

# ==== App custom properties ====
app.frontend-url=${APP_FRONTEND_URL:http://localhost:3000}
app.s3.endpoint=${MINIO_ENDPOINT:http://localhost:9000}
app.s3.bucket=fieldforce-files
app.s3.access-key=${MINIO_ACCESS_KEY:minioadmin}
app.s3.secret-key=${MINIO_SECRET_KEY:minioadmin}
fieldforcepro.geocode.google-api-key=AIzaSyAKjmBSUJ3XR8uD10vG2ptzqLJAZnOlzqI